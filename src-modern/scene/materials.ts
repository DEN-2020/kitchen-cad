import * as THREE from 'three';
const cache=new Map<string,THREE.CanvasTexture>();
export function proceduralTexture(pattern?:string,color?:string){if(!pattern||pattern==='solid')return null;const key=`${pattern}:${color}`;if(cache.has(key))return cache.get(key)!;const c=document.createElement('canvas');c.width=c.height=256;const g=c.getContext('2d')!;g.fillStyle=color||'#aaa';g.fillRect(0,0,256,256);if(pattern==='wood'){g.globalAlpha=.22;for(let y=7;y<256;y+=11){g.strokeStyle=y%22?'#5b361f':'#fff3d4';g.lineWidth=1+((y/11)%3);g.beginPath();for(let x=0;x<=256;x+=8){const yy=y+Math.sin(x*.045+y)*3+Math.sin(x*.13)*1.2;x?g.lineTo(x,yy):g.moveTo(x,yy)}g.stroke()}g.globalAlpha=1}else if(pattern==='stone'){g.globalAlpha=.2;for(let i=0;i<18;i++){g.strokeStyle=i%2?'#858585':'#ffffff';g.lineWidth=.6+(i%3)*.5;g.beginPath();let y=(i*37)%256;g.moveTo(0,y);for(let x=0;x<256;x+=16){y+=Math.sin((x+i)*.3)*8;g.lineTo(x,y)}g.stroke()}g.globalAlpha=1}else{g.globalAlpha=.16;for(let i=0;i<900;i++){g.fillStyle=i%2?'#fff':'#111';g.fillRect((i*71)%256,(i*43)%256,1.2,1.2)}g.globalAlpha=1}const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;t.wrapS=t.wrapT=THREE.RepeatWrapping;t.repeat.set(1.5,1.5);t.anisotropy=4;cache.set(key,t);return t}
