import { useEffect, useMemo, useState } from 'react';
import { KitchenScene } from './scene/KitchenScene';
import { addModule, deleteModule, deriveModel, duplicateModule, loadEditorProject, saveEditorProject, snapModuleAbsolute, updateCountertop, updateModule, type DimensionDetail, type Selection } from './domain/core';
const types=[['base','Нижний шкаф'],['sink','Шкаф под раковину'],['wall','Навесной шкаф'],['washer','Стиральная машина'],['dishwasher','Посудомойка'],['oven','Духовка'],['fridge','Холодильник']];
export function App(){const [project,setProject]=useState<any>(()=>loadEditorProject()),[selection,setSelection]=useState<Selection>(null),[inspector,setInspector]=useState(false),[focusId,setFocusId]=useState<string|null>(null),[detail,setDetail]=useState<DimensionDetail>('none'),[addOpen,setAddOpen]=useState(false);const model=useMemo(()=>deriveModel(project),[project]);const selectedModule=selection?.kind==='module'?model.modules.find((m:any)=>m.id===selection.id):null;useEffect(()=>saveEditorProject(project),[project]);const patchModule=(patch:any)=>selection?.kind==='module'&&setProject((p:any)=>updateModule(p,selection.id,patch));const remove=()=>{if(selection?.kind==='module'){setProject((p:any)=>deleteModule(p,selection.id));setSelection(null);setInspector(false);setFocusId(null)}};const duplicate=()=>selection?.kind==='module'&&setProject((p:any)=>duplicateModule(p,selection.id));const moveModule=(id:string,x:number,z:number)=>setProject((p:any)=>snapModuleAbsolute(p,id,x,z));const moveCountertop=(x:number,z:number)=>setProject((p:any)=>updateCountertop(p,{offsetX:Math.round(x/50)*50,offsetZ:Math.round(z/50)*50}));const dark=project.ui?.theme!=='light';return <div className={dark?'app dark':'app'}><header className="topbar"><button className="iconBtn">☰</button><div className="brand">Kitchen CAD <span>Three</span></div><button className="iconBtn" onClick={()=>setProject((p:any)=>({...p,ui:{...p.ui,theme:dark?'light':'dark'}}))}>◐</button></header><main className="editor"><section className="scenePanel"><KitchenScene project={project} model={model} selection={selection} setSelection={(s:Selection)=>{setSelection(s);if(!s)setInspector(false)}} focusId={focusId} detail={detail} onMoveModule={moveModule} onMoveCountertop={moveCountertop}/><div className="viewbar"><button className="active">3D</button><button onClick={()=>setDetail(v=>v==='none'?'selected':'none')}>{detail==='none'?'+ размеры деталей':'− детали'}</button></div>{selection&&<div className="selectionHud"><div><b>{selection.kind==='countertop'?'Столешница':selectedModule?.type==='sink'?'Шкаф под раковину':'Выбранный модуль'}</b><span>{selection.kind==='countertop'?`${project.countertop.length} × ${project.countertop.depth} × ${project.countertop.thickness} мм`:selectedModule?`${selectedModule.width} × ${selectedModule.height} × ${selectedModule.depth} мм`:''}</span></div><div className="hudActions"><button onClick={()=>setInspector(true)}>⚙</button>{selection.kind==='module'&&<><button onClick={duplicate}>⧉</button><button onClick={remove}>🗑</button></>}</div></div>}<button className="fab" onClick={()=>setAddOpen(true)}>＋</button>{focusId&&<button className="exitFocus" onClick={()=>setFocusId(null)}>← Вернуться в комнату</button>}</section><aside className={inspector?'inspector open':'inspector'}><div className="inspectorHead"><b>{selection?.kind==='countertop'?'Столешница':selectedModule?'Настройки модуля':'Настройки'}</b><button onClick={()=>setInspector(false)}>×</button></div>{selection?.kind==='countertop'?<div className="inspectorBody"><section><h3>Габариты</h3><div className="compactGrid"><label>Длина<input type="number" value={project.countertop.length} onChange={e=>setProject((p:any)=>updateCountertop(p,{length:+e.target.value}))}/></label><label>Глубина<input type="number" value={project.countertop.depth} onChange={e=>setProject((p:any)=>updateCountertop(p,{depth:+e.target.value}))}/></label><label>Толщина<input type="number" value={project.countertop.thickness} onChange={e=>setProject((p:any)=>updateCountertop(p,{thickness:+e.target.value}))}/></label></div></section><section><h3>Положение</h3><div className="compactGrid"><label>X<input type="number" value={project.countertop.offsetX||0} onChange={e=>setProject((p:any)=>updateCountertop(p,{offsetX:+e.target.value}))}/></label><label>Z<input type="number" value={project.countertop.offsetZ||0} onChange={e=>setProject((p:any)=>updateCountertop(p,{offsetZ:+e.target.value}))}/></label><label>Высота<input type="number" value={project.countertop.elevation||860} onChange={e=>setProject((p:any)=>updateCountertop(p,{elevation:+e.target.value}))}/></label></div></section></div>:selectedModule?<div className="inspectorBody"><section><h3>Габариты</h3><div className="compactGrid"><label>Ширина<input type="number" value={selectedModule.width} onChange={e=>patchModule({width:+e.target.value})}/></label><label>Высота<input type="number" value={selectedModule.height} onChange={e=>patchModule({height:+e.target.value})}/></label><label>Глубина<input type="number" value={selectedModule.depth} onChange={e=>patchModule({depth:+e.target.value})}/></label></div></section><section><h3>Фасад</h3><div className="segmented">{['flat','frame','glass','slatted'].map(v=><button key={v} className={selectedModule.frontStyle===v?'active':''} onClick={()=>patchModule({frontStyle:v})}>{v==='flat'?'Плоский':v==='frame'?'Рамка':v==='glass'?'Стекло':'Рейки'}</button>)}</div></section><section><h3>Focus / Edit</h3><button className="focusBtn" onClick={()=>setFocusId(selectedModule.id)}>Редактировать модуль отдельно</button><p>Остальная кухня приглушается; дальше сюда добавим полки, отдельные фасады, крепёж и внутреннюю конструкцию.</p></section></div>:<div className="empty">Выбери модуль или столешницу на сцене.</div>}</aside>{addOpen&&<div className="modalBackdrop" onClick={()=>setAddOpen(false)}><div className="addSheet" onClick={e=>e.stopPropagation()}><div className="sheetHead"><b>Добавить</b><button onClick={()=>setAddOpen(false)}>×</button></div><div className="addGrid">{types.map(([type,name])=><button key={type} onClick={()=>{const r=addModule(project,type);setProject(r.project);setSelection({kind:'module',id:r.id});setAddOpen(false)}}><span>▦</span><b>{name}</b></button>)}</div></div></div>}</main><nav className="bottomNav"><button>Комната</button><button className="active">Модули</button><button onClick={()=>setDetail(v=>v==='all'?'none':'all')}>Размеры</button><button onClick={()=>setInspector(true)}>Свойства</button></nav></div>}
