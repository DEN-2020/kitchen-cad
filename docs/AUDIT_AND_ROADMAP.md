# Kitchen CAD — аудит и план развития

Дата аудита: 2026-09-16.

## Проверенная база

- Production-сборка проходит (`tsc --noEmit` + Vite).
- 103 доменных теста проходят; покрытие загруженных тестами модулей регулярно проверяется командой
  `node --experimental-test-coverage --test tests/*.test.js`.
- `npm audit --omit=dev`: 0 известных уязвимостей.
- Production открывается на ширинах 320, 390 и desktop без ошибок загрузки.
- Основной объём JS после сборки — около 1.27 МБ без сжатия / 357 КБ gzip; крупнейший chunk — Three.js (около 734 КБ / 190 КБ gzip).

Важно: высокий процент покрытия относится почти исключительно к JavaScript-ядру. React-интерфейс,
WebGL-сцена, сохранение современного редактора и генератор отчётов автоматическими тестами почти
не покрыты.

## Статус первого цикла исправлений

Завершено 2026-09-15:

- A-001: современный загрузчик валидирует JSON и версию схемы до React-render; повреждённое
  сохранение проверено в браузере и теперь безопасно заменяется новым проектом.
- A-002: сегменты столешницы и линии стыков подключены к 3D-сцене; столешница выбирается тапом и
  показывает рассчитанную суммарную длину.
- A-003/A-004: столешница строится одной сегментной моделью в автоматическом режиме и не
  разрывается над стиральной или посудомоечной машиной.
- A-005: денежные поля показывают `EGP`, процентные — `%`.
- Часть A-007: добавлены accessible names и состояния переключателей для основных символьных
  кнопок, закрытия панели и удаления встраиваемого элемента.

Проверка после изменений: 62/62 теста, production-сборка, мобильный viewport 390×844,
восстановление после значения `{}` в `localStorage`, выбор столешницы и просмотр сметы.

## Статус второго цикла исправлений

Завершено 2026-09-15:

- A-006: добавлен безопасный импорт JSON с проверкой схемы, версии и лимита размера; невалидный
  файл показывает ошибку и не заменяет текущий проект.
- Добавлена история до 80 шагов с кнопками undo/redo и сочетаниями `Ctrl+Z`,
  `Ctrl+Shift+Z`/`Ctrl+Y`; импорт также можно отменить.
- В верхней панели отображается состояние автосохранения. Запись дебаунсится на 250 мс, а последнее
  состояние дополнительно сохраняется при закрытии страницы.

Проверка после изменений: 67/67 тестов, production-сборка и мобильный браузерный сценарий
«изменить размер → undo → redo → импортировать → отменить импорт → отклонить невалидный JSON».

## Статус третьего цикла исправлений

Завершено 2026-09-15:

- A-008: при входе в отдельное редактирование камера учитывает поворот модуля и открывает его со
  стороны реального фасада. Изменения материала, глянца и других параметров больше не сбрасывают
  вручную выбранный 3D-ракурс; при изменении размеров камера только следует за новым центром.
- A-009: глухая секция углового шкафа использует декор и основу корпуса, а не фасада. Это закреплено
  тестом, включая независимый глянец дверцы и матовый корпус.
- A-010: круглые и квадратные ножки отображаются явно. Режим «скрытые/цоколь» создаёт реальные
  регулируемые опоры, утапливает их за цоколь и показывает пояснение; при высоте 0 мм корпус стоит
  непосредственно на полу.
- A-011: выбор декоров заменён на визуальные образцы; дерево, камень и крапление получили более
  детальные процедурные текстуры. Фасады имеют отдельный переключатель «матовый/глянцевый», а
  физические материалы, отражения, тени и освещение лучше показывают разницу покрытий.
- A-012: рисунок декора и его оттенок разделены, поэтому пользовательский цвет теперь одинаково
  применяется к однотонным и текстурным фасадам всех типов. Образцы собраны в компактную
  горизонтальную ленту, у оттенка есть явное значение и сброс, а настройки отдельных фасадов
  свёрнуты и больше не мешают обычному выбору. Общая настройка материала выравнивает фасады,
  сохраняя индивидуальную сторону и количество петель.

Проверка после изменений: 71/71 тест, `tsc --noEmit`, production-сборка и браузерные сценарии
«повёрнутый на 180° глухой угловой модуль → вход в редактор со стороны фасада → ручной поворот
камеры → включение глянца без изменения ракурса» и «текстура дерева → пользовательский оттенок →
сброс оттенка → индивидуальный декор → общий декор с сохранением стороны петель».

## Статус четвёртого цикла исправлений

Завершено 2026-09-15:

- A-013: нижняя панель теперь удаляется из DOM после нажатия крестика и гарантированно освобождает
  сцену на мобильном экране. Закрытие проверено на viewport 390×844.
- A-014: смета явно показывает, что считает всю кухню. Листы разных основ, декоров, оттенков и
  толщин больше не объединяются в одну заготовку; добавлена раскрываемая разбивка по фактическим
  материалам и явная подсветка активного ценового сценария.
- A-015: в настройках появился стиль кухни для новых модулей и отдельная отменяемая команда
  применения декора, оттенка и глянца ко всей мебели. Индивидуальные параметры модуля, размеры,
  петли, ножки и ручки при этом сохраняются.
- A-016: в разобранном виде снова остаются видимыми ручки и опоры, а подписи и разнесение
  применяются только к деталям раскроя. Глухой нижний модуль шириной от 1000 мм получает шесть
  опор вместо четырёх.
- A-017: каталог получил поиск и явное обозначение техники как отдельного проёма. Пересечение
  стиральной или посудомоечной машины с угловым корпусом выдаёт отдельное предупреждение;
  небезопасный шаблон «техника внутри углового шкафа» не добавляется.
- A-018: в редакторе глухого угла пояснены назначение глухой панели, реальный проём доступа и
  нормальный выступ накладного фасада на величину зазора плюс толщину фасада.
- A-019: визуальная система редактора стала спокойнее и ближе к рабочему CAD-инструменту:
  карточные рамки убраны с обычных секций, полей и строк сметы, иерархия строится фоном,
  интервалами и тонкими разделителями. Рамки сохранены только для ошибок и действительно
  обособленных объектов; мобильная навигация и активные состояния получили единый акцент.
- A-020: поиск каталога по умолчанию свёрнут в компактную кнопку и занимает полную строку только
  во время ввода. Активный запрос сохраняется в короткой кнопке и очищается отдельно.
- A-021: глухой угловой модуль получил зеркальный выбор стороны проёма. Вместе с дверцей
  перестраиваются глухая корпусная панель, петли и ручка. Мойка добавляется прямо из параметров
  модуля, центрируется над доступной секцией, ограничивается её полезной шириной и убирает полку;
  при смене стороны мойка следует за проёмом.

Проверка после изменений: 78/78 тестов, `tsc --noEmit`, production-сборка и мобильные сценарии
«смета → смена сценария → закрытие панели», «поиск глухого угла → ширина 1200 мм → ручка и ножки →
разобранный вид», «компактный поиск → фильтр “глухой”», «дверца слева/справа → добавить мойку →
мойка следует за проёмом», «стиль кухни → графитовый глянец → применить ко всей мебели» и
визуальная проверка обновлённой тёмной/светлой темы на ширинах 320, 412 и 1200 px.

## Статус пятого цикла исправлений

Завершено 2026-09-16:

- A-022: верхний и нижний глухие угловые модули используют одну зеркальную схему проёма. Между
  дверцей и глухой корпусной панелью добавлена отдельная узкая монтажная стойка под специальные
  петли глухого угла. Стойка входит в напил как деталь корпуса, зеркалится вместе с проёмом, а
  фасад хранит ссылку на деталь крепления и правильную сторону петель. Ширина стойки настраивается
  в диапазоне 28–150 мм; значение по умолчанию — 70 мм.
- A-023: у всех шкафов, кроме ящиков, появился независимый режим «без фасада». Он удаляет дверцы,
  ручки и петли из 3D, деталировки и фасадной части сметы, но сохраняет корпус, полки, опоры и
  монтажную стойку углового шкафа. Старое значение `doorCount = 0` по-прежнему означает
  автоматическое число створок, поэтому сохранённые проекты не меняют поведение.
- Редактор отдельных фасадов показывает фактическую сторону петель из рассчитанной модели, а не
  общий шаблон. В режиме открытого корпуса скрыты неактуальные поля фасада и остаются доступны
  материалы корпуса.

Проверка после изменений: 83/83 теста, `tsc --noEmit`, production-сборка и мобильный сценарий
390×844 «верхний глухой угол → фасад слева/справа → проверка стороны петель → без фасада →
открытый корпус со стойкой и без фасадных настроек». Ошибок браузера нет; остаётся известное
нефатальное предупреждение Three.js об устаревшем `THREE.Clock`.

## Статус шестого цикла исправлений

Завершено 2026-09-16:

- A-024: смета разделяет матовые и глянцевые фасады на разные партии материалов и применяет к ним
  разные редактируемые цены. Переключение финиша фасада меняет выбранный продукт и итог всей
  кухни; разбивка показывает матовые и глянцевые фасады отдельно.
- A-025: правила перемещения собраны в `MODULE_PLACEMENT` и единую функцию
  `resolvePlacementPose`. Нижние, верхние, угловые модули, техника и элементы комнаты проходят
  одинаковую привязку к стене, автоповорот, привязку к соседям своего уровня и обязательное
  ограничение повёрнутого габарита внутри комнаты. Ограничение применяется уже во время drag, а
  не только после отпускания. Реестр автоматически покрывает каждый тип из
  каталога и проверяется тестом.
- A-026: у столешницы появился тот же компактный выбор декора и оттенка, что у мебели, плюс
  матовый/глянцевый финиш. 3D больше не подменяет выбранный рисунок принудительным камнем: дерево,
  однотонный цвет, камень и крапление отображаются согласно выбранному декору.
- A-027: в панели вида добавлены независимые режимы «все дверцы» и «стенд». Дверцы плавно
  открываются вокруг фактической стороны петель; ручки и декоративные элементы движутся вместе с
  фасадом. Режим стенда плавно вращает камеру и не изменяет геометрию, раскрой или смету.
- A-028: египетский прайс-лист нормализован на EGP/м². Каждый продукт хранит собственные формат
  листа, толщину, тип основы, финиш и диапазон рынка; стоимость считается как площадь деталей ×
  цена/м² × 1.15, а целые листы показываются отдельно как ориентир закупки. Материалы корпуса и
  дверок выбираются независимо глобально и для отдельного модуля. Панель вида получила единый
  набор SVG-иконок на `currentColor`, корректный в обеих темах.
- A-029: у верхнего и нижнего глухого угла монтажная перегородка петель теперь действительно
  повёрнута перпендикулярно фасаду, как боковина, и не перекрывает глубину углового объёма. Для
  обоих вариантов доступны 1 или 2 створки и полное отключение фасада; диагональные углы также
  поддерживают 1/2 створки, а у L-угла явно показано конструктивное ограничение на две связанные
  панели. Выбранный продукт материала теперь попадает не только в смету, но и в CSV и печатные
  карты деталей.

Проверка после изменений: 95/95 тестов, `tsc --noEmit`, production-сборка и браузерные сценарии
360×800 «проверить иконки в обеих темах → сменить ценовой сценарий → добавить верхний глухой угол →
переключить 1/2 створки → проверить перпендикулярную монтажную перегородку».
Ошибок браузера нет; два известных предупреждения Three.js остаются нефатальными.

Архитектурный срез после цикла: правила размещения и анимационная математика теперь находятся в
чистом JS-ядре и покрыты тестами. Основные оставшиеся риски — `App` (строки 223–3598),
`buildProject` (цикломатическая сложность 40, cognitive 90) и неподключённый `AppV2` как второй
ложный entry point. Их декомпозицию лучше делать отдельным циклом после фиксации текущего поведения
сценарными тестами, не смешивая с производственными правилами мебели.

## Статус седьмого цикла исправлений

Завершено 2026-09-16:

- A-030: режим «стенд» больше не делает полный оборот камеры за стенами. Чистая функция
  `cinematicCameraPose` строит фронтальную кинематографическую траекторию: боковой проход,
  приближение/отдаление и верхний/нижний ракурс при постоянном наведении на кухню. Во время
  показа ручное вращение блокируется, а тёплый источник света медленно проходит вместе с камерой,
  чтобы глянцевые фасады читались по блику.
- A-031: редактор модуля разделён на компактные вкладки с единым SVG-стилем: размер, корпус,
  фасад, цвет и техника. Для нижнего шкафа, шкафа под мойку и глухого нижнего угла добавлен
  настоящий проём под стиральную/посудомоечную машину. В обычном 600-мм модуле это свободный
  проём без боковин, дна, полок, фасада, цоколя и ножек; в глухом углу эти детали убираются только
  из доступной секции, а глухая секция сохраняет дно и полки. Проверяются ширина, глубина, высота,
  верхний зазор и конфликт мойки с техникой; варочная поверхность получает предупреждение о
  паспортных зазорах и вентиляции.
- A-032: смета получила предварительный двумерный раскрой каждой фактической партии материала.
  Алгоритм учитывает собственный формат листа, обрезку края, ширину пропила и направление
  древесной текстуры, показывает карты целых листов, процент использования, размеры полезных
  прямоугольных остатков и оценку количества деталей заданного размера, которые ещё войдут в
  остатки. Цена по формуле `площадь × EGP/м² × запас` сохранена отдельно от справочной суммы
  закупки целыми листами, чтобы округление закупки не искажало сравнительную смету.

Проверка после изменений: 103/103 теста, `tsc --noEmit`, production-сборка и мобильный сценарий
412×915 «глухой нижний угол → вкладка техники → посудомойка 600×815×570 → проверка проёма и
полезной глухой секции → конфликт с мойкой», а также «смета → карты листов → остатки → проверка,
что ещё помещается» и визуальная проверка движения камеры в режиме стенда. Ошибок браузера нет;
известные предупреждения Three.js остаются нефатальными.

Архитектурный вывод: математика траектории и раскрой листов вынесены в независимые чистые модули с
тестами. При этом добавление проёмов под технику ещё сильнее подчёркивает необходимость следующего
рефакторинга: разделить `buildProject` на генераторы семейств корпусов и вынести вкладки редактора
из крупного `App.tsx` в отдельные компоненты до расширения библиотеки техники и производственных
ограничений.

## P0 — исправить немедленно

### A-001. Повреждённое локальное сохранение ломает запуск

Воспроизведено в браузере: значение `{}` в `kitchen-cad-three-v1` проходит загрузчик, после чего
валидация во время React-render выбрасывает ошибку «Неподдерживаемая версия проекта». Результат —
полностью пустая страница. Загрузчик должен декодировать и валидировать данные до первого render,
а при ошибке безопасно запускать новый проект.

Критерии готовности:

- битый JSON, неподдерживаемая схема и неполный объект не приводят к пустому экрану;
- валидный проект продолжает загружаться без потерь;
- автосохранение возвращает явный результат, а не скрывает ошибку полностью.

### A-002. Столешница и её стыки рассчитаны, но не отображаются

`deriveModel` создаёт `countertop-segment` и `countertopJoints`, но `SceneContent` не рендерит ни
`CountertopSegment`, ни `CountertopJointVisual`. Старый объект `CT-01` перед этим удаляется. Поэтому
пользователь видит открытые корпуса, не может выбрать столешницу и не видит типы стыков.

Критерии готовности:

- все рассчитанные сегменты видны и выбираются в 3D;
- линии и подписи стыков видны для L-/П-образных конфигураций;
- выбор столешницы открывает её реальные параметры.

## P1 — следующий рабочий приоритет

### A-003. Автоматическая столешница разрывается над встраиваемой техникой

Список поддерживаемых типов содержит стиральную и посудомоечную машины, но фильтр сразу исключает
их. В результате техника шириной 600 мм между шкафами разбивает непрерывный прогон на две части.

### A-004. Смешаны старая одиночная и новая сегментная модели столешницы

Современный редактор принудительно переводит `lengthMode` в `manual`, показывает редактируемую
длину и смещения старого `CT-01`, но сегментный генератор эти поля не использует. Проверки старой
геометрии могут не соответствовать тому, что должно отображаться в L-/П-образной кухне.

### A-005. Поля сметы показывают неверные единицы

Цена листа, стоимость работ и проценты визуально подписаны как миллиметры из-за значения по
умолчанию в `NumberField`. Денежные поля должны показывать валюту, проценты — `%`.

### A-006. Нет импорта JSON, истории изменений и понятного статуса сохранения

Проект можно скачать, но нельзя восстановить из файла через интерфейс. Ошибка `localStorage`
скрывается, undo/redo отсутствуют. Для мобильного редактора это высокий риск потери работы.

### A-007. Базовая доступность неполная

У кнопок закрытия инспектора, удаления встраиваемого элемента и символьных переключателей размеров
нет понятных accessible names. WebGL-объекты недоступны с клавиатуры и не продублированы списком.

## P2 — точность, тестируемость и масштабирование

- Добавить проверки уникальности fixture ID и допустимого типа целевого модуля.
- Корректно строить столешницы для островов и нескольких параллельных прогонов, а не определять
  положение только по углу поворота и ближайшей стене.
- Добавить производственные правила: зазоры техники, вентиляция, зоны мойки/варочной панели,
  минимальные остатки материала вокруг вырезов.
- Завершить арабскую локализацию отчётов и всех вручную заданных строк интерфейса.
- Покрыть современный React/WebGL-слой сценарными тестами: запуск, выбор, редактирование, импорт,
  экспорт и восстановление после повреждённого сохранения.
- Добавить библиотеку оптимизированных GLB-скинов техники с процедурным fallback; геометрия моделей
  остаётся только визуальной и не влияет на размеры, спецификацию и зазоры.
- Добавить автоматический регрессионный тест камеры: вход к модулю с поворотами 0/90/180/270° и
  сохранение orbit-ракурса после изменения параметров.
- Разделить `App.tsx` (сейчас около 3600 строк) на независимые панели и хуки; заменить
  критичные `any` доменными типами. Это главный текущий риск сопровождения интерфейса.
- Разбить монолитный `buildProject` по семействам шкафов и вынести общие операции раскроя в
  небольшие чистые функции; текущие метрики (cyclomatic 40 / cognitive 90) уже делают добавление
  новых конструкций рискованным.
- Добавить проверочные fixtures для каждого семейства угловых шкафов и сравнение итогового
  раскроя с утверждёнными производственными картами до объявления расчёта производственным.
- Удалить или архивировать неподключённые `AppV2.tsx`, старый `src/ui/app.js` и текстовые фрагменты
  после проверки, что они не нужны для истории миграций.
- Убрать повторные `filter/find` по деталям и объектам внутри циклов; для 64 модулей заранее строить
  индексы `moduleId -> parts/objects`.

## P3 — улучшения продукта и эксплуатации

- Добавить именование проекта, список локальных проектов и явное создание нового проекта.
- Добавить Error Boundary с экраном восстановления и скачиванием резервной копии.
- Проверить обновление Three.js/React Three после стабилизации текущего функционала; сейчас есть
  нефатальное предупреждение об устаревшем `THREE.Clock`.
- Добавить лимиты производительности для bundle и мобильный smoke-тест в CI.
- Добавить security headers для статического хостинга и документировать формат/миграции JSON.

## Очерёдность работ

1. ✅ Надёжная загрузка сохранения и восстановление запуска (A-001).
2. ✅ Возврат столешницы и стыков в 3D (A-002).
3. ✅ Непрерывный прогон над техникой и единая сегментная модель (A-003, A-004).
4. ✅ Правильные единицы сметы и accessible names (A-005, часть A-007).
5. ✅ Импорт JSON, статус сохранения, undo/redo (A-006).
6. Производственные проверки и полноценные сценарные тесты.
7. Декомпозиция `App` и `buildProject`, удаление ложных entry point после закрепления поведения.
8. Оптимизация проходов по деталям и внедрение версионируемого прайс-листа поставщиков.
