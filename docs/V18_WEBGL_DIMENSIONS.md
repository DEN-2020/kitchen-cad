# V18 WebGL exploded dimensions

Exploded part dimensions are rendered as WebGL sprites instead of DOM/HTML overlays so they move with parts and are included in canvas PNG/PDF captures. Whole-module dimensions are hidden while exploded. Camera framing is preserved while changing explode distance.
