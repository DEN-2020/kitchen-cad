body.addEventListener('click',e=>{const s=e.target.closest('[data-select]');if(s){selected=s.dataset.select;render();return}const add=e.target.closest('[data-add]');if(add){const m=createModule(add.dataset.add);project.modules.push(m);selected=m.id;activePanel='modules';render();return}const a=e.target.closest('[data-action]')?.dataset.action;if(!a)return;if(a==='copy'){const m=structuredClone(currentModule());m.id=newId();project.modules.push(m);selected=m.id}else if(a==='remove'){if(project.modules.length>1){project.modules=project.modules.filter(m=>m.id!==selected);selected=project.modules[0].id}}else if(a==='add'){activePanel='add'}else if(a==='dimensions'){project.ui.showDimensions=!project.ui.showDimensions}else if(a==='language'){project.ui.language=lang()==='ru'?'en':'ru'}else if(a==='json'){download(JSON.stringify(project,null,2),'kitchen-project.json','application/json')}else if(a==='csv'){download(cutListCSV(buildProject(project).parts),'cut-list.csv','text/csv;charset=utf-8')}render()});
$$('[data-panel]').forEach(b=>b.addEventListener('click',()=>openPanel(b.dataset.panel)));$('#addBtn').onclick=()=>openPanel('add');$('#menuBtn').onclick=()=>openPanel('project');$('#closeDrawer').onclick=closePanel;$('#backdrop').onclick=closePanel;$('#langBtn').onclick=()=>{project.ui.language=lang()==='ru'?'en':'ru';render()};$('#dimensionBtn').onclick=()=>{project.ui.showDimensions=!project.ui.showDimensions;render()};$('#fitBtn').onclick=()=>{zoom=1;yaw=.72;render()};$$('[data-view]').forEach(b=>b.onclick=()=>{view=b.dataset.view;$$('[data-view]').forEach(x=>x.classList.toggle('active',x===b));render()});
canvas.addEventListener('pointerdown',e=>{canvas.setPointerCapture(e.pointerId);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(pointers.size===2){const p=[...pointers.values()];lastDistance=Math.hypot(p[0].x-p[1].x,p[0].y-p[1].y)}});canvas.addEventListener('pointermove',e=>{if(!pointers.has(e.pointerId))return;const old=pointers.get(e.pointerId),dx=e.clientX-old.x;pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(pointers.size===1&&view==='iso'){yaw+=dx*.008;render()}else if(pointers.size===2){const p=[...pointers.values()],dist=Math.hypot(p[0].x-p[1].x,p[0].y-p[1].y);if(lastDistance){zoom=Math.max(.55,Math.min(2.4,zoom*dist/lastDistance));render()}lastDistance=dist}});const up=e=>{pointers.delete(e.pointerId);if(pointers.size<2)lastDistance=0};canvas.addEventListener('pointerup',up);canvas.addEventListener('pointercancel',up);canvas.addEventListener('wheel',e=>{e.preventDefault();zoom=Math.max(.55,Math.min(2.4,zoom*(e.deltaY>0?.92:1.08)));render()},{passive:false});
addEventListener('resize',()=>render());if(innerWidth>=900){drawer.classList.add('open');activePanel='modules'}render();
